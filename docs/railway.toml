[build]
builder = "NIXPACKS"
buildCommand = "cd .. && pnpm install && cd docs && pnpm build"

[deploy]
startCommand = "cd docs && pnpm preview --host 0.0.0.0 --port $PORT"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_VERSION = "20"
