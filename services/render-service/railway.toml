[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && pnpm install"

[deploy]
startCommand = "cd services/render-service && pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_VERSION = "20"
