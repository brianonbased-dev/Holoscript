[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.mcp-server"

[deploy]
startCommand = "node packages/mcp-server/dist/http-server.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "holoscript-mcp"
